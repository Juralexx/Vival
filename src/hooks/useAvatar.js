export function avatar(props) {
    return ({
        backgroundImage: `url(${props})`,
        backgroundRepeat: "no-repeat",
        backgroundPosition: "center",
        backgroundSize: "cover"
    })
}

export function coverPicture(props) {
    return ({
        backgroundImage: `url(${props})`,
        backgroundRepeat: "no-repeat",
        backgroundPosition: "center",
        backgroundSize: "cover"
    })
}

export function projectPicture(props) {
    return ({
        backgroundImage: `url(${props})`,
        backgroundRepeat: "no-repeat",
        backgroundPosition: "center",
        backgroundSize: "cover"
    })
}
